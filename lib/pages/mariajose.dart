import 'package:flutter/material.dart';

class mariajose extends StatelessWidget {
  const mariajose({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}