import 'package:flutter/material.dart';

class ManuelFlores extends StatelessWidget {
  const ManuelFlores({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
