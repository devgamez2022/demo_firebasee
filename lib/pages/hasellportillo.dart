import 'package:flutter/material.dart';

class HasellPortillo extends StatelessWidget {
  const HasellPortillo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}