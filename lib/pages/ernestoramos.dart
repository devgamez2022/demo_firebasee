import 'package:flutter/material.dart';

class ErnestoRamos extends StatelessWidget {
  const ErnestoRamos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}