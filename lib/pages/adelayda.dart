import 'package:flutter/material.dart';

class adelayda extends StatelessWidget {
  const adelayda({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}