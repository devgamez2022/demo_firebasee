import 'package:flutter/material.dart';

class LeviMendoza extends StatelessWidget {
  const LeviMendoza({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      ArgumentError
    );
  }
}

